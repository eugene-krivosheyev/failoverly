{
  "name": "failoverly-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --port=8000 index.html",
    "serve": "bun --port=8000 index.html",
    "build": "bun run assets && bun tooling/build.mjs",
    "preview": "bun tooling/preview.mjs",
    "assets": "bun tooling/brand-assets.mjs",
    "audit": "bun tooling/audit.mjs",
    "audit:3g": "bun tooling/audit.mjs --3g",
    "format": "prettier --write index.html privacy.html css/main.css scripts/*.js tooling/*.mjs package.json site.config.json vercel.json README.md",
    "format:check": "prettier --check index.html privacy.html css/main.css scripts/*.js tooling/*.mjs package.json site.config.json vercel.json README.md",
    "check": "bun run format:check && bun run build && bun tooling/check-build.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "fontkit": "^2.0.4",
    "lighthouse": "^13.4.1",
    "prettier": "^3.9.6"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "engines": {
    "bun": ">=1.3.11"
  }
}
