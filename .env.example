# BUILD TIME: public canonical origin. The default is already site.config.json.
# Copy to .env only if you need a local override. Never put secrets in SITE_URL.
SITE_URL=https://failoverly.app

# TODO (deploy credentials): set VERCEL_TOKEN, VERCEL_ORG_ID and VERCEL_PROJECT_ID
# as GitHub production environment secrets, NOT browser or site-build variables.
# See .github/workflows/deploy-production.yml and README.md.

# RUNTIME: the current site is static and requires no runtime environment values.
# TODO (future forms): once a provider is chosen, add its API key and list/audience
# ID to Vercel's server-side Production environment and implement a server endpoint.
# Exact variable names depend on that provider. Nothing reads those settings yet.
# Do not embed provider credentials in HTML, browser JS or public build variables.
# TODO (future forms): an external API origin also needs permission in connect-src
# in tooling/security.mjs. A server endpoint on the site's own origin is allowed.
